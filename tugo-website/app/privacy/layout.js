export const metadata = {
  title: "Privacy & Terms",
  description:
    "How I handle the information you send me, the booking terms, and what you can expect — written in plain language.",
};

export default function Layout({ children }) {
  return children;
}
