export const metadata = {
  title: "About Tugi",
  description:
    "I grew up traveling Mongolia's countryside with my grandparents. Now I host small-group trips on routes I already know and love.",
};

export default function Layout({ children }) {
  return children;
}
