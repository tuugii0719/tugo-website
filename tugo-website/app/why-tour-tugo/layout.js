export const metadata = {
  title: "Why Tugi",
  description:
    "Not a tour company. A side quest with someone who actually knows the country. Here's how I run these trips and what to expect.",
};

export default function Layout({ children }) {
  return children;
}
